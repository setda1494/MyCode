let arr = new Array();
let product1={}
class product2
{
    constructor(naem,price)
    {
        this.naem = name
        this.price = price
    }
    print()
    {
        console.log(`${this.naem}${this.price}`);
    }
}
let p1 = new product2(`banana`,1200);
console.log(p1.naem);
console.log(p1.price);

for(let i of products)
{
    i.print
}